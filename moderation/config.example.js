// Copy this file to `config.js` and fill in your project's values.
// Both are safe to expose in a browser — the dashboard signs in as a STAFF user
// and Row-Level Security is what actually gates access. Never put the
// service-role key here.
window.CM_CONFIG = {
  SUPABASE_URL: "https://YOUR-project-ref.supabase.co",
  SUPABASE_ANON_KEY: "your-anon-public-key",
};
